// Compiler Theory and Design
// Dr. Duane J. Jarc

// This file contains the enumerated type definition for tokens

enum Tokens {
    RELOP = 256, ADDOP, ARROW, MULOP, ANDOP, 
    BEGIN_, BOOLEAN, BOOL_LITERAL, CASE, ELSE,
     END, ENDCASE, ENDIF, ENDREDUCE, FUNCTION,
    INTEGER, IF, IS, NOTOP, OTHERS, OROP, REAL,
    REAL_LITERAL, REDUCE, RELOP, REMOP, RETURNS,
    THEN, IDENTIFIER, INT_LITERAL, WHEN};
