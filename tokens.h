// Compiler Theory and Design
// Dr. Duane J. Jarc

// This file contains the enumerated type definition for tokens

enum Tokens {RELOP = 256, ADDOP, ARROW, MULOP, ANDOP, BEGIN_, BOOLEAN, CASE, ELSE, END, ENDCASE,
 ENDIF, ENDREDUCE, FUNCTION, INTEGER, IF, IS, OTHERS, REAL, REDUCE, RETURNS, THEN, IDENTIFIER,
 INT_LITERAL, WHEN};
